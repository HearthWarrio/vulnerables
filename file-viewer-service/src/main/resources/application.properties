# vuln-portal-java/file-viewer-service/src/main/resources/application.properties

server.port=8086

# JWT secret must match Auth Service
jwt.secret=VerySecretJwtKey12345

# Base directory for files to view
viewer.base-dir=pages

# (Ensure 'pages' directory exists in volume or classpath for demo)
